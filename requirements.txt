# scikit-learn >= 1.4 is required: RandomForestRegressor only gained native
# support for missing values in that release, and this project relies on it.
scikit-learn>=1.4
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
joblib>=1.3
tabulate>=0.9
